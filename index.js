// Add any homepage scripting here!
//want to return collection of elements

var acc = document.getElementsByClassName("accordion");
var i;

for (i = 0; i < acc.length; i++) {
  acc[i].addEventListener("click", function () {
    this.classList.toggle("active");
    var panel = this.nextElementSibling;
    if (panel.style.display === "block") {
      panel.style.display = "none";
    } else {
      panel.style.display = "block";
    }
  });
}

//on click, you can view whatever is in the accordion
//the reason we put this in a loop is if there is multiple elements of type accordion
